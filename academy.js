import { CLASS_SEED } from './catalog.js';import { api } from './shared.js';import { money } from './config.js';let classes=CLASS_SEED;try{const r=await api('/classes');if(r.classes?.length)classes=r.classes}catch{}const grid=document.querySelector('[data-class-grid]');grid.innerHTML=classes.map(c=>`<article class="class-card"><div class="class-meta"><span class="pill">${c.track}</span><span class="pill">${c.level}</span></div><h3>${c.title}</h3><p>${c.summary}</p><div class="class-foot"><div><strong>${c.price!=null?money(c.price):'Price coming soon'}</strong><br><small>${c.duration||'Schedule coming soon'}</small></div>${c.available?`<a class="btn btn-rose" href="class-registration.html?id=${encodeURIComponent(c.id)}">Register</a>`:`<span class="pill">Registration not open</span>`}</div></article>`).join('');
