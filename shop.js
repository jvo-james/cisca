import { PRODUCT_SEED } from './catalog.js';import { api,saveCart,getCart,toast } from './shared.js';import { money } from './config.js';let products=PRODUCT_SEED;try{const r=await api('/products');if(r.products?.length){const map=new Map(PRODUCT_SEED.map(p=>[p.id,p]));r.products.forEach(p=>map.set(p.id,{...(map.get(p.id)||{}),...p}));products=[...map.values()]}}catch{}
const grid=document.querySelector('[data-product-grid]'),rail=document.querySelector('[data-category-rail]'),search=document.querySelector('[data-product-search]'),availableOnly=document.querySelector('[data-available-only]');const categories=['All','Makeup','Nails','Brow & Lash','Hair'];let cat=new URLSearchParams(location.search).get('category')||'All';if(!categories.includes(cat))cat='All';
function render(){rail.innerHTML=categories.map(c=>`<button class="category-chip ${c===cat?'active':''}" data-cat="${c}">${c}</button>`).join('');const q=search.value.trim().toLowerCase();const rows=products.filter(p=>(cat==='All'||p.category===cat)&&(!q||`${p.name} ${p.category}`.toLowerCase().includes(q))&&(!availableOnly.checked||p.available&&p.price!=null));grid.innerHTML=rows.map(p=>`<article class="product-card"><a class="product-art" href="product.html?id=${encodeURIComponent(p.id)}">${p.image?`<img src="${p.image}" alt="${p.name}">`:`<span class="initial">${p.name.charAt(0)}</span>`}<span class="status-badge">${p.available&&p.price!=null?'Available':'Coming soon'}</span></a><div class="product-info"><small>${p.category}</small><h3><a href="product.html?id=${encodeURIComponent(p.id)}">${p.name}</a></h3><p>${p.description||'Beauty kit essential selected for the Cisca shop.'}</p><div class="product-foot"><strong>${p.price!=null?money(p.price):'Price not set'}</strong><button data-add="${p.id}" ${!(p.available&&p.price!=null)?'disabled':''}>${p.available&&p.price!=null?'Add to bag':'Unavailable'}</button></div></div></article>`).join('')||'<div class="empty-state">No products match that search.</div>';rail.querySelectorAll('button').forEach(b=>b.onclick=()=>{cat=b.dataset.cat;render()});grid.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>{const p=products.find(x=>x.id===b.dataset.add);const cart=getCart(),found=cart.find(x=>x.id===p.id);found?found.qty++:cart.push({id:p.id,name:p.name,price:p.price,qty:1,image:p.image,category:p.category});saveCart(cart);toast(`${p.name} added to your bag.`)})}
search.addEventListener('input',render);availableOnly.addEventListener('change',render);render();
