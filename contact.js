import { api,toast } from './shared.js';const form=document.querySelector('[data-contact-form]');form.onsubmit=async e=>{e.preventDefault();const btn=form.querySelector('button');btn.disabled=true;try{await api('/contact',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(form)))});form.reset();toast('Message sent. We will get back to you.')}catch(err){toast(err.message)}finally{btn.disabled=false}};
