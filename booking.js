import { SERVICE_SEED } from './catalog.js';import { api,toast } from './shared.js';import { money } from './config.js';
let services=SERVICE_SEED;try{const r=await api('/services');if(r.services?.length)services=r.services}catch{}
const picker=document.querySelector('[data-service-picker]'),form=document.querySelector('[data-booking-form]');let selected=[];const wanted=new URLSearchParams(location.search).get('service');
function render(){picker.innerHTML=services.filter(s=>s.active!==false).map(s=>`<button type="button" class="service-option ${selected.includes(s.id)?'active':''}" data-id="${s.id}"><strong>${s.name}</strong><small>${s.category} · ${s.price!=null?money(s.price):'Price on request'}</small></button>`).join('');picker.querySelectorAll('button').forEach(b=>b.onclick=()=>{selected=selected.includes(b.dataset.id)?selected.filter(x=>x!==b.dataset.id):[...selected,b.dataset.id];render();summary()})}
function summary(){const rows=services.filter(s=>selected.includes(s.id));document.querySelector('[data-sum-services]').textContent=rows.length?rows.map(r=>r.name).join(', '):'Choose a service';document.querySelector('[data-sum-total]').textContent=money(rows.reduce((n,r)=>n+Number(r.price||0),0));document.querySelector('[data-sum-date]').textContent=form.date.value||'Not selected';document.querySelector('[data-sum-location]').textContent=form.locationType.value.replace(' appointment','')}
render();if(wanted&&services.some(s=>s.id===wanted)){selected=[wanted];render();summary()}form.date.min=new Date().toISOString().slice(0,10);form.addEventListener('input',summary);form.addEventListener('submit',async e=>{e.preventDefault();if(!selected.length)return toast('Choose at least one service.');const btn=document.querySelector('[data-submit]');btn.disabled=true;btn.textContent='Preparing payment...';try{const fd=Object.fromEntries(new FormData(form));const result=await api('/booking/start',{method:'POST',body:JSON.stringify({...fd,serviceIds:selected})});location.href=result.authorizationUrl}catch(err){toast(err.message);btn.disabled=false;btn.textContent='Continue to secure payment'}});summary();
